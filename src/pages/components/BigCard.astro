---
import { Image } from "astro:assets";
import Button from "./Button.astro";
const { placeholder, buttonText, order } = Astro.props;
---

<section class="feature">
  <Image src={placeholder} alt="Beskrivende tekst" class="placeholder" />
  <div class="content">
    <h2>Lorem ipsum dolor sit amet consectetur.</h2>
    <p>Lorem ipsum dolor sit amet consectetur. Purus ut eget odio nulla aliquam. Sed in scelerisque elit morbi maecenas nec cras parturient.</p>
    <Button buttonText={buttonText} />
  </div>
</section>

<style define:vars={{ order }}>
  .placeholder {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }
  .content {
    order: var(--order);
    display: flex;
    justify-content: center;
    flex-direction: column;
    align-items: flex-start;
    gap: 1rem;
  }
  .feature {
    padding-top: 2rem;
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    grid-column: 2 / span 1;
    gap: 5rem;
  }
</style>
